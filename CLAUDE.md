# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

diaryctl — a diary management CLI tool (project not yet initialized).

## Active Technologies
- Go 1.22+ (001-diary-crud)
- Pluggable interface with Markdown file + SQLite (Turso) (001-diary-crud)
- Go 1.24 (go1.24.12 toolchain) + Cobra (CLI), Bubble Tea + Bubbles + Lipgloss (TUI), Viper (config) (003-daily-view-picker)
- Pluggable — Markdown files and SQLite/libSQL backends via `storage.Storage` interface (003-daily-view-picker)

## Recent Changes
- 001-diary-crud: Added Go 1.22+
